{{template "base" .}}


{{ define "content"}}
<div class="container">
    <div class="row" >
        <div class="col">
            <h1>Service Page</h1>
            <p>Landing Page</p>

            <p>Came from the template: {{index .StringMap "test"}}</p>
        </div>
    </div>
</div>
{{end}}